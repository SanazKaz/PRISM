# TargetDiff PPO fine-tuning config
# Run with:
#   python scripts/train.py --config configs/targetdiff_ppo.yaml \
#       --datadir /path/to/crossdocked_data --seed 42

run_identifier: '1_6_26_TD_CD_MULTI_OBJ'
logdir: 'Log_Results'

wandb_params:
  mode: 'online'
  entity: 'sanazkazeminia97'
  project: 'TargetDiff_CD_multi_objective'

model_type: targetdiff

# TargetDiff UniTransformer layer names (9 layers total, indices 0-8).
# We freeze the bulk of the network and fine-tune:
#   - v_inference head                    (atom type output)
#   - last three transformer layers (6, 7, 8) = 1/3 of the 9-layer stack
freeze_except:
  - 'v_inference'
  # - 'refine_net.base_block.5' # new
  # - 'refine_net.base_block.6'
  - 'refine_net.base_block.7'
  - 'refine_net.base_block.8'

model:
  # Path to the TargetDiff pretrained checkpoint (.pt, native format with
  # key 'model' containing the state_dict).
  checkpoint: 'checkpoints/targetdiff_pretrained_models/targetdiff_pretrained_diffusion.pt'

  # Run process_data.py with --model targetdiff to produce this dataset.
  # That pipeline uses FeaturizeProteinAtom (6 elements + 20 AA + backbone = 27 dims),
  # matching the pretrained checkpoint exactly — no layer re-initialisation needed.
  protein_feat_dim: 27
  ligand_atom_types: 13   # add_aromatic encoding, 13 classes

  total_timesteps: 1000   # full TargetDiff chain length (sigmoid beta schedule)

# -----------------------------------------------------------------------
# Data
# -----------------------------------------------------------------------
datadir: '/data/stat-cadd/wolf7055/PRISM/data/cross_dock/processed_crossdock_targetdiff'
dataset: 'crossdock'
batch_size: 32
num_workers: 4
gpus: 4
n_epochs: 1

enable_progress_bar: False
num_sanity_val_steps: 0

# -----------------------------------------------------------------------
# PPO algorithm knobs
# -----------------------------------------------------------------------
ppo:
  num_outer_epochs: 70
  num_inner_epochs: 2
  n_steps: 64                     # molecules generated per rollout
  ligand_chunk_size: 64           # reduce if OOM 
  batch_size: 64
  clip_range: 0.1
  entropy_coef: 0.001
  kl_coef: 0.05 # new kl coef
  max_grad_norm: 1.0
  gradient_accumulation_steps: 42 
  train_timesteps: 700            # PPO trains on the last 700 of 1000 reverse steps
  target_kl: null
  lr: 3.0e-7 # reduced from 3.0e-5 to avoid instability
  num_nodes_lig: 27

# -----------------------------------------------------------------------
# Rewards  (same interface as DiffSBDD runs)
# -----------------------------------------------------------------------
docking_params:
  dataset_info: custom

reward_params:
  target_name: 'BRD4_BD1'
  aggregation: 'weighted_sum'
  rewards:
    qed: 0.0
    sa_score: 0.0
    lipinski: 0.0
    bertz: 0.0
    sucos: 0.0
    interaction_fingerprints: 0.0
    feature_density: 0.0
    geometry_checks: 0.1
    flatness_checks: 0.0
    aromatic_counter: 0.0
    aromatic_anchor: 0.0
    multi_features: 0.0
    silly_walks: 0.0
    property_2d: 0.0
    smina_docking: 0.4
    custom_qed: 0.3
    custom_sa_score: 0.2
    penalised_logp: 0.0
  reward_paths:
    feature_density: '/data/stat-cadd/wolf7055/PRISM/data/BRD4_BD1/hotspot_analysis/BRD4_BD1_hotspot_data.pkl'
    aromatic_anchor: 'src/prism/reward/scoring/reward_data/ampc_hotspot_global_centered_eps0.5_min10_aromatic5.pkl'
    multi_features: 'src/prism/reward/scoring/reward_data/ampc_hotspot_global_centered_eps0.5_min10_aromatic5.pkl'
    silly_walks: 'src/prism/reward/scoring/reward_data/chembl_drugs.smi'
    property_2d: 'src/prism/reward/scoring/reward_data/propeties_ref.json'

# -----------------------------------------------------------------------
# Evaluation
# -----------------------------------------------------------------------
eval_epochs: 10
visualize_sample_epoch: 99999
visualize_chain_epoch: 999999
eval_params:
  n_eval_samples: 20
  eval_batch_size: 20
  smiles_file: '/data/stat-cadd/wolf7055/PRISM/data/cross_dock/processed_crossdock_noH_full_temp/train_smiles.npy'
  n_visualize_samples: 0
  keep_frames: 0

# Gradient / layer-diagnostics logging (off by default; toggle per-run).
grad_logging:
  enabled: false
  every_n_epochs: 1
  log_histogram: true
  per_block_histogram: true
  max_hist_elements: 1000000
